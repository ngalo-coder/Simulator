[build]
  command = "npm run build"
  publish = "build"

[context.production.environment]
  # This should be the URL of your deployed API gateway
  REACT_APP_API_URL = "https://ai-patient-sim-gateway.onrender.com"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
